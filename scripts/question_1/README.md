# Question 1: SDTM DS Domain Creation using {sdtm.oak}

- `question_1.R` contains the code for generating a SDTM DS Domain data set
- `question_1_logfile.txt` contains the log output from `question_1.R`
- See `output/question_1.R` for the SDTM DS Domain data set file generated from the `question_1.R` script